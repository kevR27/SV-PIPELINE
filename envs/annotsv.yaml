# AnnotSV binary plus the VCF utilities used by downstream Snakemake rules.
name: annotsv
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::annotsv=3.5.10
  - bioconda::bcftools=1.20
  - bioconda::htslib=1.20
