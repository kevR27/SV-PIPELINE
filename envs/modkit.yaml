# modkit itself is configured as an external executable in config_lrs.yaml.
# This environment only supplies htslib/tabix for indexing modkit BGZF output.
name: modkit
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::htslib=1.24
