# CLI command is `modkit`; Bioconda package is `ont-modkit`.
# Pinned to the version currently used on the analysis server.
name: modkit
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::ont-modkit=0.4.4
  - bioconda::htslib=1.24
