# Main long-read SV discovery environment.
# Keep this independent from the needLR compatibility query environment.
name: sniffles2
channels:
  - conda-forge
  - bioconda
dependencies:
  - bioconda::sniffles=2.8.1
